apiVersion: v1
kind: Pod

metadata:
  name: pod-second
  namespace: kubefirst 
  labels:
    name: test
spec:
  containers:
  - name: test
    image: bitbull/webserver
    ports:
    - containerPort: 80    
